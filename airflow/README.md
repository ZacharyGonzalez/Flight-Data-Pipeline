### Prerequisites
Set up an "Amazon Web Services" connection in Airflow
with a valid Access Key / Secret Key ID.
Provide CLUSTER_ID in `dag.py`.

### Running the DAG
Open the Airflow dashboard and run the DAG.